spring:
  datasource:
    url: jdbc:mysql://localhost:13306/msc_prod
    username: msc_prod_user
    password: passwd
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false